---
import { Image } from "astro:assets";
import CustomImage  from "./CustomImage.astro"
import CustomText from "./CustomText.astro"
const {text, image, imageLocation, imageLocationDesktop, color} = Astro.props
enum ImageLocationDesktop {
    Right = "right",
    Left= "left",
}
//TODO find media query for desktop
---

{/*for mobile*/}
<div class="flex lg:hidden flex-col lg:flex-row lg:justify-between visible xl:invisible">
        <div class="lg:w-1/2">
            <Image class="py-5" src={image} alt="yoga image"/>
        </div>
        <h class="self-center pb-20 text-4xl sm:text-5xl md:text-6xl">{text}</h>  
</div>

{/*for desktop
TODO tailwind doesnt apply the color grey but it should*/}
<div class=`hidden lg:flex flex-col lg:flex-row lg:justify-between bg-${color}`>
    <!-- <h1>{`bg-${color}`}</h1> -->
    {imageLocationDesktop === ImageLocationDesktop.Left && 
        <CustomImage src={image}/>
        <CustomText text={text} />
    }
    {imageLocationDesktop === ImageLocationDesktop.Right && 
        <CustomText text={text} />
        <CustomImage src={image}/>
    }
</div>

